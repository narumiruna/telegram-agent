services:
  telegramagent:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        PYTHON_VERSION: "3.14"
    image: telegramagent:local
    env_file:
      - .env
    command: ["--verbose"]
    user: "${UID:-1000}:${GID:-1000}"
    environment:
      HOME: /tmp
    init: true
    restart: unless-stopped
    volumes:
      - ./.agents:/app/.agents
